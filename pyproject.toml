[project]
name = "mcp-bridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "langchain-ollama>=0.3.6",
    "langchain-openai>=0.3.28",
    "mcp-use",
    "pip>=25.2",
    "pydantic>=2.11.7",
]

[tool.uv.sources]
mcp-use = { git = "https://github.com/mcp-use/mcp-use" }
