services:
  mcp-bridge:
    build: .
    ports:
      - "8000:8000"
    environment:
      HOST: "0.0.0.0"
      PORT: "8000"
      DEBUG: "False"
      MCP_HOST: "http://gateway:9011/mcp"
    env_file:
      - .env
    volumes:
#      - ./logs:/app/logs
      - /Users/roberto/python-test/mcp-national-rail:/app/servers/mcp-national-rails:ro
#      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      - gateway
  gateway:
    image: docker/mcp-gateway:dind
    privileged: true
    ports:
      - "9011:9011"
    command:
      - --transport=sse
      - --servers=duckduckgo
      - --port=9011
      - --memory=512Mb

